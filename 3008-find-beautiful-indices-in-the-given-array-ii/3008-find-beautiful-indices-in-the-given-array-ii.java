class Solution {
    public List<Integer> beautifulIndices(String s, String chars, String ignoredParam, int k) {
        List<Integer> result = new ArrayList<>();
        int n = s.length();

        // Example logic: add every index (replace with actual logic)
        for (int i = 0; i < n; i++) {
            result.add(i);
        }

        return result;
    }
}
